import { partOne, partTwo } from './puzzle';

describe('day 01', () => {
  test('Part One', () => {
    expect(partOne('')).toEqual(undefined);
  });

  test('Part Two', () => {
    expect(partTwo('')).toEqual(undefined);
  });
});
